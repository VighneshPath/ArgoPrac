apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "helmArgo.fullname" . }}-test-connection"
  labels:
    {{- include "helmArgo.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "helmArgo.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
